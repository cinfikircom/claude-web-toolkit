/**
 * BROKEN-BY-DESIGN config for SEO-OS validation.
 * NOTE: This is a controlled-failure fixture. Do not "fix" or optimize.
 * (No image optimization config, no headers, no compression tuning — intentional.)
 */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
